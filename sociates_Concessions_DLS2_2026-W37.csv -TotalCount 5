warning: in the working copy of 'components/ProfessionalViewsV10.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/components/ProfessionalViewsV10.jsx b/components/ProfessionalViewsV10.jsx[m
[1mindex 479676a..a6e52ac 100644[m
[1m--- a/components/ProfessionalViewsV10.jsx[m
[1m+++ b/components/ProfessionalViewsV10.jsx[m
[36m@@ -27,7 +27,7 @@[m [mfunction useDbRows(organizationId, kind){[m
           .order("period_end",{ascending:true})[m
           .limit(10000);[m
         if(kind==="iadc") query=query.not("iadc","is",null);[m
[31m-        if(kind==="concessions") query=query.not("concessions","is",null);[m
[32m+[m[41m        [m
         const {data,error}=await query;[m
         if(error)throw error;[m
         const rows=(data||[]).filter((row)=>{[m
[36m@@ -163,12 +163,13 @@[m [mexport function DirectConcessionsView({organizationId,onOpenDriver}){[m
   if(load.loading)return <Loading text="Loading concessions directly from saved driver metrics…"/>;[m
   if(load.error)return <ErrorBox error={load.error}/>;[m
 [m
[32m+[m[32m  const concessionRows=load.rows.filter(r=>n(r.concessions)!=null);[m
   const weeks=contiguousWeeks(load.rows,range);[m
   const weekSet=new Set(weeks);[m
[31m-  const weekTotals=weeks.map(w=>load.rows.filter(r=>r.week_label===w).reduce((s,r)=>s+(n(r.concessions)||0),0));[m
[31m-  const presentSet=new Set(load.rows.map(r=>r.week_label));[m
[32m+[m[32m  const weekTotals=weeks.map(w=>concessionRows.filter(r=>r.week_label===w).reduce((s,r)=>s+(n(r.concessions)||0),0));[m
[32m+[m[32m  const presentSet=new Set(concessionRows.map(r=>r.week_label));[m
   const m=new Map();[m
[31m-  for(const row of load.rows){[m
[32m+[m[32m  for(const row of concessionRows){[m
     if(!weekSet.has(row.week_label))continue;[m
     const d=row.drivers||{},id=row.driver_id||trid(d);[m
     const cur=m.get(id)||{id,driver:d,byWeek:{},row};[m
